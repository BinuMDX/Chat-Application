# For Supabase with Connection Pooling (Transaction Mode - Port 6543)
# IMPORTANT: Add &statement_cache_size=0 to avoid prepared statement errors with PgBouncer
# Replace YOUR_PASSWORD with your actual password (URL encode special characters!)
DATABASE_URL="postgresql://postgres.PROJECT_REF:YOUR_PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres?pgbouncer=true&statement_cache_size=0"

# For Supabase Direct Connection (Session Mode - Port 5432)
# Required for running migrations
DIRECT_URL="postgresql://postgres.PROJECT_REF:YOUR_PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres"

# Local Development (Alternative)
# DATABASE_URL="postgresql://user:password@localhost:5432/chatapp"

# JWT Configuration
JWT_SECRET="supersecretkey"
JWT_EXPIRES_IN="1d"